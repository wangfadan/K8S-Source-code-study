# svc-deploy 代码详解与二次开发指南

这是一套用 Go 写的 **MySQL Group Replication (MGR) 高可用集群**部署器，跑在 Kubernetes 上。
本文按"整体 → 逐文件逐段 → 二次开发"的顺序讲解。

---

## 一、整体架构

### 1.1 包结构（全部 `package main`，5 个文件）

```
client.go      K8s 客户端入口（读 kubeconfig 建 clientset）
main.go        命令路由(up/down/purge/status) + bootstrap 编排
controller.go  控制器：reconcile 循环 + leader election + 加组逻辑
resources.go   K8s 资源对象构造 + 增删 + Pod 辅助 + 拓扑展示
mysql.go       对单个 MySQL 实例下发的 MGR 相关 SQL 原语
```

### 1.2 调用关系

```
main()
├─ up:  ensureResources ──> waitForPods ──> setupAndBootstrap ──> runWithLeaderElection
│        (建K8s对象)        (等Pod Ready)   (配MGR+bootstrap)     (常驻控制器)
├─ down:  deleteResources
├─ purge: deleteResources + deletePVCs
└─ status: topologyString

setupAndBootstrap()
  └─ bootstrapAndJoinAll() / 补齐加组  ──> 调 mysql.go 的 SQL 原语

runWithLeaderElection() ──> runController() ──> reconcile() 每5s + informer 事件
  └─ reconcile(): 读组视图 -> 找PRIMARY打master标签 -> 掉队成员rejoin
```

### 1.3 两条数据通路（理解全图的关键）

1. **K8s 通路**：程序通过 `clientset`（client.go）操作 Namespace/STS/Service/ConfigMap/Pod/Lease。
2. **MySQL 通路**：程序通过 `database/sql`（mysql.go）直接用 **Pod IP:3306** 连每个 MySQL 实例下发 SQL。

控制器不参与 MGR 选主，只观测 MGR 选出的 PRIMARY 并把 `role=master` 标签打上去，
让 `master` Service 的端点跟随——这就是"自动故障转移 + 客户端路由"的实现方式。

---

## 二、逐文件逐段详解

### 2.1 client.go —— K8s 客户端

```go
GetClient()  // 读 /root/.kube/config，返回 *kubernetes.Clientset
```
- 全程序唯一一个 K8s client 工厂。路径**硬编码**为 `/root/.kube/config`，意味着设计上是在集群 master 节点以 root 运行。
- 二次开发要换地方跑，改这里（见 3.2）。

---

### 2.2 main.go —— 入口与编排

**`main()`（L25）**：参数路由
- `up`：四步流水线，最后一步 `runWithLeaderElection` **前台常驻**，Ctrl-C 退出。
- `down`：删业务资源，保留 namespace + PVC（数据持久）。
- `purge`：down + 删 PVC（reclaimPolicy=Delete 连带删 PV 和节点目录，全量重置）。
- `status`：打印拓扑。

**`setupAndBootstrap()`（L80）**：bootstrap 编排，核心流程：
1. 列 Pod → 过滤 Ready → 按 ordinal 排序。
2. 逐个 `waitMySQLUp`（轮询 ping）。
3. `buildSeeds` 拼当前各 Pod IP；对每个成员 `ensurePlugin` + `setGroupSeeds` + `setRecoveryCreds`。
   > 注意：恢复账号 repl **不在**这里建，只在 bootstrap 成员上建（见 2.3），否则每个成员各自 CREATE USER 会产生本机 uuid 的 GTID，互不为子集导致加组失败。
4. **探测组是否已在跑**：遍历 Pod 读组视图，有 `ONLINE`/`RECOVERING` 成员才算"在跑"（不能只看 `len>0`，插件加载但未 START 时会有一行本地 OFFLINE）。
5. 没在跑 → `bootstrapAndJoinAll`；已在跑 → 对未加组成员 `joinOnce`。
6. `waitForAllOnline` 等全部 ONLINE（超时只告警，不阻断，靠控制器继续收敛）。

**`waitMySQLUp()`（L161）**：deadline 轮询 ping，3s 一次。

---

### 2.3 controller.go —— 控制器与加组逻辑

**常量/结构（L28-37）**
- `reconcileInterval = 5s`：reconcile 节拍。
- `controller`：持有 client + `masterName`（记忆当前主库，仅用于变化感知/日志）+ `lastTopology`（拓扑变化才打印，防刷屏）。

**`ordinal()`（L40）**：从 `mysqlha-2` 取出 `2`。

**`buildSeeds()` / `buildAllowlist()`（L53/L64）**：把各 Ready Pod 的 IP 拼成 `ip:33061` 列表 / 纯 IP 列表。每次加组前用最新 IP 重设，规避 XCom 解析主机名。

**`bootstrapAndJoinAll()`（L76）**——引导全新组，最关键也最易踩坑：
1. 按 ordinal 排序，收集每个成员的 `server_uuid` + `gtid_executed`。
2. `pickBootstrapIP` 挑 bootstrap 成员（见 2.5：选 gtid 是所有人超集的；全全新则取序号最小；发散则取最长并告警）。
3. `stopGR` 清残留半启动状态。
4. **全新成员 `RESET MASTER`**：mysql:8.0 首次初始化会产生几条本机 uuid 的 DDL GTID，各 Pod 互不为子集，必须清掉。带历史数据的成员不 RESET（会丢数据）。
5. **`ensureReplUser` 只在 bootstrap 成员建**：恢复账号作为干净的第一笔事务，随组复制传播给其余成员。
6. 设 seeds / allowlist / recovery 凭据 → `bootstrapGroup` → 等 ONLINE。
7. 对其余成员 `joinOnce`。

**`joinOnce()`（L146）**——让一个非成员加入运行中的组（幂等可重试）：
- 已在 RECOVERING/ONLINE 不打扰；否则 stopGR → ensurePlugin → 全新则 RESET MASTER → 设 seeds/allowlist/凭据 → `startGR`。

**`rejoin()`（L176）**：`joinOnce` 的控制器包装，带日志。

**`reconcile()`（L185）**——收敛循环，自动故障转移的核心：
1. 列 Ready Pod，建 `uuid↔Pod` 双向映射。
2. 找一个 GR 在跑的成员读组视图；**无在线成员则触发 bootstrap**。
3. **找 PRIMARY，给它打 `role=master`**（master Service 端点随即跟过去）。
4. 在线成员打 `role=slave`，并**刷新 ip_allowlist**（Pod 重启换 IP 后能被接纳）。
5. 不在组内/ERROR 的成员 → `rejoin`。

**`waitForAllOnline()`（L276）**：轮询组视图直到 want 个 ONLINE。

**`runController()`（L302）**——驱动 reconcile：
- 用 SharedInformerFactory 监听 Pod 的 Add/Update/Delete。
- `syncCh` 容量 1 做去抖（多次事件合并一次 reconcile）。
- informer 事件 + 5s ticker 都往 syncCh 投，主 select 消费 → `reconcile` + `printTopology`。

**`runWithLeaderElection()`（L357）**——Lease 选主，保证同一时刻只有一个控制器实例：
- Lease 名 `mysqlha-controller`，LeaseDuration 15s / RenewDeadline 10s / RetryPeriod 2s。
- 拿到 Lease 才 `runController`；失去则停止。

---

### 2.4 resources.go —— K8s 资源构造与增删

**常量区（L22-48）**：集群名、端口（mysql 3306 / group 33061）、`local-path` 存储类、镜像、组名 UUID、root/repl 密码、role 标签值。**二次开发改参数基本都在这里。**

**辅助（L51-64）**：`logf`（带 `[mgrha]` 前缀日志）、`fail`、`podFQDN`（拼稳定域名供日志）。

**构造器（L68-269）**
- `buildNamespace` / `buildSecret`（存 root/repl 密码）。
- `buildConfigMap`（L85）：**MGR 全局参数都在这**。gtid/binlog/log_slave_updates、`plugin_load_add`、`group_name`、`start_on_boot=OFF`、`single_primary_mode=ON`、`ip_allowlist`（含 K8s Pod 网段 100.64.0.0/10）、`binlog_expire_logs_seconds`。变量用 `loose-` 前缀防插件未加载时 mysqld 拒启。
- `buildHeadlessService`：ClusterIP=None，给每 Pod 稳定 DNS。
- `buildMasterService`（L139）：selector 带 `role=master`，端点跟着标签走。
- `initContainer`（L150）：按 `POD_IP` 写 `server-id.cnf`（server-id 由 IP 算整数）和 `mgr-local.cnf`（`local_address` + `report_host` 都设成 Pod IP）。
- `mysqlContainer`（L183）：root 密码来自 Secret，挂 conf/data 卷，ReadinessProbe 用 `mysqladmin ping`。
- `buildStatefulSet`（L212）：Pod 反亲和（每节点一成员）+ 容忍 control-plane 污点（dev 环境只有 2 worker + 1 master）+ local-path PVC 模板。

**增删（L274-366）**
- `ensureResources`：幂等创建全部对象（已存在跳过；STS 已存在则 patch template）。
- `waitNamespaceActive`：等 namespace Active。
- `deleteResources`：删业务资源，保留 namespace + PVC。
- `deletePVCs`：删 PVC（连带 PV 和节点目录）。

**Pod 辅助（L370-444）**
- `listMySQLPods` / `podReady` / `filterReady` / `findPod`。
- `setPodRole`（L410）：merge patch 打 role 标签——**master Service 跟随的落点**。
- `currentMasterName`：找当前带 `role=master` 的 Pod。
- `waitForPods`：等 want 个 Pod Ready（6 分钟超时，含 PVC provisioning + 首次初始化）。

**拓扑展示（L448-521）**：`topologyString` 汇总组成员状态 + Pod 角色，供 status/控制器打印。

---

### 2.5 mysql.go —— MGR 的 SQL 原语

**连接（L18-61）**
- `dsn`：拼 root 连接串，`readTimeout=60s`（START GROUP_REPLICATION 连种子/恢复可能阻塞数秒，8s 会误判）。
- `openDB` / `pingMySQL` / `execAll`（逐条执行，mysql 驱动默认不支持多语句）。

**MGR SQL（L65-134）**
- `ensurePlugin`：查 `information_schema.PLUGINS`，没 ACTIVE 就 `INSTALL PLUGIN`（兜底）。
- `ensureReplUser`：建 repl 账号，`mysql_native_password` 避免恢复通道走 RSA。
- `setRecoveryCreds`：`CHANGE REPLICATION SOURCE TO ... FOR CHANNEL 'group_replication_recovery'`。
- `setGroupSeeds` / `setIPAllowlist`：动态 `SET GLOBAL`，用当前 Pod IP。
- `bootstrapGroup`：`bootstrap_group=ON → START GROUP_REPLICATION → OFF`。
- `startGR` / `stopGR`。

**组视图查询（L136-231）**
- `groupMember` 结构：memberID(uuid) / host / port / state / role。
- `groupMembers`：查 `performance_schema.replication_group_members`。
- `serverUUID`：取 `@@server_uuid`（**成员身份标识，持久化在 PV 的 auto.cnf**，Pod 重建换 IP 仍是同一成员）。
- `gtidExecuted`：取 `@@global.gtid_executed`，挑 bootstrap 成员用。
- `waitMemberOnline`：轮询组视图直到某 uuid 的成员 ONLINE 或 ERROR。

**bootstrap 成员挑选（L235-351）**——决定数据不丢的关键
- `podGtid`：name/ip/uuid/gtid 四元组。
- `freshStateByStr` / `freshState`：判断成员是否"全新"——gtid 空，或只含本机 uuid 的事务。有别的主库 uuid 事务则说明已有组数据，不能 RESET。
- `pickBootstrapIP`：
  - 全全新 → 取序号最小。
  - 否则用 MySQL `GTID_SUBSET(a,b)` 判断谁是谁的超集，选数据最全的。
  - 没有严格超集（发散/脑裂）→ 取 gtid 串最长者，返回 `divergent=true` 供告警。

---

## 三、二次开发指南

### 3.1 四个扩展点（改哪类东西去哪个文件）

| 想改的东西 | 去哪改 | 说明 |
|---|---|---|
| 集群参数（副本数/密码/镜像/存储/组名/端口） | `resources.go` 常量区 L22-48 | 改完 `down`+`purge` 后重新 `up` |
| MySQL 配置（MGR 模式/白名单/binlog） | `resources.go` `buildConfigMap` L85 | 注意 `loose-` 前缀 |
| K8s 对象形态（加 sidecar/改探针/加标签） | `resources.go` 各 builder | STS 已存在时只 patch template |
| 新增 SQL 操作（备份/建库/监控查询） | `mysql.go` 加新函数 | 复用 `openDB`/`execAll` |
| 控制器行为（新的收敛逻辑/新的自动化） | `controller.go` `reconcile` L185 | reconcile 是收敛中枢 |
| 新增子命令 | `main.go` switch L34 | 加 case + 实现函数 |
| 换运行位置/集群 | `client.go` `GetClient` L9 | 改 kubeconfig 来源 |

### 3.2 常见改造配方

**① 改副本数**：改 `replicas`（resources.go:32）。注意三点：MGR 要多数派（建议奇数）；Pod 反亲和要 N 个节点；多主模式除外。

**② 改密码**：改 `rootPwd`/`replPwd`（resources.go:42-44）。Secret 只在不存在时创建，改密码后必须 `purge` 全量重置才生效。

**③ 换镜像**：改 `mysqlImage`（resources.go:37）。`initContainer` 也用同一镜像跑 sh，换精简镜像要确保有 sh。

**④ 换存储类/大小**：改 `storageClass`/`dataSize`（resources.go:35-36）。`volumeClaimTemplates` 不可变，已存在 STS 改存储类需 `purge` 重建。

**⑤ 改 kubeconfig 路径 / 在别处跑**：把 `client.go` 的硬编码路径换成环境变量或标准加载规则：
```go
import "k8s.io/client-go/tools/clientcmd"
func GetClient() *kubernetes.Clientset {
    loading := clientcmd.NewDefaultClientConfigLoadingRules()      // 自动读 KUBECONFIG / ~/.kube/config
    cfg, err := clientcmd.NewNonInteractiveDeferredLoadingClientConfig(loading, &clientcmd.ConfigOverrides{}).ClientConfig()
    if err != nil { panic(err) }
    c, err := kubernetes.NewForConfig(cfg)
    if err != nil { panic(err) }
    return c
}
```
> 但即便改了路径，**从集群外仍连不通 Pod IP**（MySQL 通路需要）。要么在 master 节点跑，要么把程序做成 Pod/Job 跑进集群（见 3.4）。

**⑥ 多主模式（multi-primary）**：ConfigMap 里 `single_primary_mode=OFF` + `enforce_update_everywhere_checks=ON`。但 `reconcile` 里"找唯一 PRIMARY 打 master 标签"的逻辑会失效（多主没有单一 PRIMARY）——需要重写 master 路由策略（如改用 round-robin Service，或仍选一个写入口）。这是较大的改动。

**⑦ 加业务库/用户初始化**：bootstrap 成员 ONLINE 后，在 `setupAndBootstrap` 末尾（main.go:154 前）加一步，对 PRIMARY 执行 `CREATE DATABASE`/`CREATE USER`/`GRANT`。注意只在一个成员执行，组复制会传播。

**⑧ 加备份**：在 `mysql.go` 加 `dumpPrimary(ip)`（`mysqldump` 或基于 GTID 的逻辑备份），在 main.go 加 `backup` 子命令，对当前 PRIMARY 执行。

**⑨ 加监控**：两种思路——(a) 在 `mysqlContainer` 加 mysqld-exporter sidecar + ServiceMonitor；(b) 在 `reconcile` 里周期性查 `performance_schema` 上报。

**⑩ 改 reconcile 节拍**：改 `reconcileInterval`（controller.go:29）。太快增加 K8s/MySQL 压力，太慢故障转移感知慢。

### 3.3 调试技巧

- **看控制器在干什么**：`go run . up` 的 `[mgrha]` 日志会打印每次选主/加组/拓扑变化。
- **单独看拓扑**：另开终端 `go run . status`，不动控制器。
- **手动触发故障转移**：`kubectl delete pod mysqlha-0 -n mysqlha`（删主库 Pod），观察控制器日志：MGR 选新 PRIMARY → 控制器重打 master 标签 → master Service 端点切换。
- **直接连 MySQL 排查**：`kubectl exec -it mysqlha-0 -n mysqlha -- mysql -uroot -p`，然后 `SELECT * FROM performance_schema.replication_group_members;`。
- **SQL 原语单测**：mysql.go 的函数都是"给 IP 下发 SQL"，可临时写个 `main.go` 子命令对单 Pod 调用，快速验证。

### 3.4 把程序跑进集群（推荐的生产化方向）

物理机跑 `go run` 不适合长期运行。生产化做法：
1. 写 `Dockerfile`（多阶段构建：`golang` 编译 → `scratch`/`distroless` 运行）。
2. 把二进制做成 Deployment/Deployment + ServiceAccount（用 InClusterConfig 取代 kubeconfig 文件——`client.go` 改用 `rest.InClusterConfig()`）。
3. 这样程序以 Pod 形式跑在集群里，**Pod IP 天然可达**，也不用挂 kubeconfig。
4. `up`/`down` 等一次性编排动作可改成 init 或用 Job 触发，常驻部分只留 `runWithLeaderElection`。

### 3.5 必须记住的坑（改代码时别破坏这些不变量）

1. **恢复账号 repl 只在 bootstrap 成员建**，别在每个成员各自 CREATE USER——会产生互不为子集的 GTID。
2. **`RESET MASTER` 只对全新成员**（`freshState` 为真），带历史数据的成员 RESET 会丢数据。
3. **成员身份是 `server_uuid`（PV 持久化），不是 server-id**。别把身份判断建立在 server-id 或 Pod 名上。
4. **纯 IP 通信**：seeds/local_address/allowlist 都用 Pod IP，别图省事写 FQDN（Xcom 解析主机名有坑）。
5. **`report_host=POD_IP`** 必须保留，否则恢复连 donor 时 `MEMBER_HOST` 是不可解析短名。
6. **ip_allowlist 要含 K8s Pod 网段**（默认 100.64.0.0/10），否则成员间 XCom 互拒。
7. **判断"组在跑"要看 ONLINE/RECOVERING**，不能只看 `replication_group_members` 有行（插件加载未 START 时有本地 OFFLINE 行）。
8. **`start_on_boot=OFF`**：别改成 ON，否则鸡生蛋（Pod 起来时 seeds/凭据还没配好就自动加组会失败）。
9. **master Service 靠 `role=master` 标签跟随**，别试图用其它方式固定主库端点。
10. **kubeconfig 路径 + Pod IP 可达性**：换运行位置时两个一起解决。
